import { defineStore } from 'pinia'


export const useCaseStore = defineStore({
    id: 'case',
    state: () => ({
        // name: 'Eduardo',
        // /** @type {boolean} */
        // isAdmin: true,
    }),
    actions: {
    },
})
