import { defineStore } from 'pinia'


export const useModuleStore = defineStore({
    id: 'module',
    state: () => ({
        // name: 'Eduardo',
        // /** @type {boolean} */
        // isAdmin: true,
    }),
    actions: {
    },
})
